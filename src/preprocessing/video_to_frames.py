"""video_to_frames.py
Convert video streams to frames (placeholder).
"""


def video_to_frames(video_path, out_dir):
    """Extract frames from `video_path` into `out_dir` (placeholder)."""
    return 0


if __name__ == "__main__":
    print("video_to_frames placeholder")
